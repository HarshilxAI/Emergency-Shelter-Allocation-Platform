MASTER PROMPT — ESAP ADMIN DASHBOARD FULL INTEGRATION
======================================================

You are working on my Final Year Project:

Emergency Shelter Allocation Platform (ESAP)

The existing project already has a backend, database, frontend and allocation logic. Your task is to complete and integrate the ADMIN MODULE.

I have provided:
1. Multiple screenshots showing the EXACT approved Admin UI.
2. The reference HTML file containing the Admin page structure and interactions.

IMPORTANT:
The screenshots and provided HTML are the visual/UX source of truth.

DO NOT redesign the Admin dashboard.
DO NOT introduce a new visual style.
DO NOT replace the approved UI with your own interpretation.
DO NOT unnecessarily change spacing, typography, layout, colors, buttons, modals, navbar, tables or page structure.

The Admin UI is LOCKED.

Your job is to make the existing Admin interface fully functional and correctly connected to the existing ESAP backend/database.

======================================================
1. APPROVED ADMIN STRUCTURE
======================================================

The Admin module must contain exactly these five sections:

1. Dashboard
2. Shelters
3. Requests
4. Allocations
5. History

The existing navbar/header, theme toggle, administrator indicator and sign-out control shown in the screenshots must be preserved.

Both Light Mode and Dark Mode must work correctly.

Do not remove any existing Admin functionality shown in the screenshots.

======================================================
2. DASHBOARD
======================================================

Preserve the exact dashboard design shown in the screenshots.

It contains:

- Total shelters
- Available shelters
- Free spaces
- Active requests
- Shelter capacity table
- Requests needing attention

The values must NOT remain hardcoded.

Connect them to the real backend/database.

Dashboard statistics must dynamically reflect:

- shelter count
- available shelter count
- total free capacity
- active/pending emergency requests
- current shelter occupancy
- current allocation state

Shelter capacity table must use real database data.

Requests needing attention must use real request data.

Any change in shelter capacity, request status or allocation must automatically be reflected in the dashboard after refresh/reload and preferably through appropriate state updates.

======================================================
3. SHELTERS
======================================================

Preserve the exact Shelters page shown in the screenshots.

Required functionality:

- Search shelter by name/address
- Filter by status
- Add Shelter
- View / Edit
- Shelter details modal

Shelter data must come from PostgreSQL through the backend API.

Each shelter should support:

- Name
- Address
- Total capacity
- Occupied capacity
- Free spaces
- Status
- Latitude
- Longitude
- Facilities
- Coordinator
- Phone
- Last updated

The following statuses must work consistently:

- Available
- Limited
- Full
- Temporarily closed

FREE SPACES MUST NEVER be independently hardcoded.

It must be calculated consistently:

freeSpaces = totalCapacity - occupiedCapacity

unless the existing backend architecture already uses a different validated calculation.

The View/Edit modal must display the complete shelter information shown in the screenshots.

"Add Shelter" must be a real working operation.

"Edit" must actually update the database.

After adding/editing a shelter:

- database must update
- UI must update
- dashboard statistics must update
- capacity values must remain consistent
- history/audit entry should be created where appropriate

======================================================
4. REQUESTS
======================================================

Preserve the exact Requests UI.

Required functionality:

- Search request ID/disaster/location
- Status filter
- Priority filter
- View pending request
- Open request
- Complete request details modal

Request details must include:

- Request ID
- User
- Disaster type
- Priority
- Total people
- Children
- Senior citizens
- Women
- Disabled people
- Submitted time
- Status
- Area
- Live coordinates
- Requested facilities
- Live location map

The request data must come from the real backend/database.

Do NOT leave the demo request objects from the reference HTML as the final data source.

======================================================
5. REAL MAP INTEGRATION
======================================================

This is IMPORTANT.

The placeholder maps shown in the reference HTML are only visual prototypes.

DO NOT keep the fake CSS/placeholder map.

Use the project's actual map technology:

OpenStreetMap + Leaflet.

Maps must be real and functional.

For request details:

- display the request's actual latitude/longitude
- show the request location marker
- allow proper map rendering inside the modal
- ensure the map resizes correctly when the modal opens
- ensure dark/light mode does not break the map

For allocation review:

- show request location
- show assigned shelter location
- display appropriate markers
- correctly represent the actual coordinates from the database

For history operational context:

- display meaningful location/context where applicable

Do not use fake decorative lines or fake coordinates.

======================================================
6. ALLOCATIONS
======================================================

Preserve the exact Allocations page shown in the screenshots.

Required sections:

- Active allocations
- Awaiting allocation
- Successful matches
- Current allocations

Each current allocation must have a Review action.

The Allocation Review modal must display:

- Request ID
- Group size
- Priority
- Assigned shelter
- Shelter capacity/free spaces
- Distance
- Suitability score
- Allocation time
- Allocation status
- Request/shelter map

Required actions:

A. KEEP ALLOCATION

When Admin clicks "Keep allocation":

1. Confirm/approve the current allocation.
2. Update allocation status in database.
3. Update request status appropriately.
4. Update shelter occupancy/capacity.
5. Prevent double allocation.
6. Create an audit/history record.
7. Refresh affected Admin UI data.

B. CHANGE SHELTER

When Admin clicks "Change shelter":

1. Retrieve suitable alternative shelters using the existing allocation/scoring engine.
2. Display available alternatives.
3. Show relevant information such as:
   - shelter name
   - distance
   - free capacity
   - suitability
   - facilities/status
4. Allow Admin to select an alternative.
5. Update the allocation in database.
6. Correctly release/recalculate the previous shelter capacity.
7. Reserve/update the new shelter capacity.
8. Update request/allocation status.
9. Create a history/audit record.
10. Refresh dashboard, shelter, request and allocation data.

Do NOT replace this with a simple alert/message.

The allocation engine already existing in the project should be reused rather than creating a second conflicting algorithm.

======================================================
7. HISTORY
======================================================

Preserve the exact History page shown in the screenshots.

Required functionality:

- Search request/shelter/action
- Action filter
- Date/time
- Request
- Action
- Performed by
- Details
- History details modal
- Operational context
- Export history

History must come from a real backend audit/history table or the project's existing audit mechanism.

Do NOT use hardcoded history arrays as the final implementation.

Every important administrative operation should generate appropriate history records, including:

- allocation approved
- allocation created
- shelter reassigned
- request closed
- shelter marked full
- shelter created
- shelter updated
- allocation changed
- other important administrative actions

======================================================
8. EXPORT HISTORY
======================================================

The screenshot/reference requires:

"Export history"

This must generate a real downloadable Excel file.

DO NOT simply rename a CSV file as .xlsx.

Generate a proper .xlsx file containing real history records.

Include useful columns such as:

- Date/Time
- Request ID
- Action
- Shelter
- Performed By
- Status
- Relevant Details

Use the project's existing backend architecture and dependencies where possible.

======================================================
9. AUTHENTICATION / ADMIN SECURITY
======================================================

The Admin pages must remain protected.

Only authenticated Admin users should be able to access:

/admin
/admin/shelters
/admin/requests
/admin/allocations
/admin/history

Do not rely only on frontend route protection.

Backend APIs must also verify authenticated Admin authorization.

A normal user must not be able to call Admin APIs directly and retrieve or modify Admin data.

Preserve the existing ESAP authentication architecture rather than introducing an unrelated authentication system.

Sign out must perform the actual logout/session/token cleanup.

======================================================
10. BACKEND/API REQUIREMENTS
======================================================

Before writing new APIs, inspect the existing backend.

Reuse existing:

- database models
- PostgreSQL schema
- authentication
- allocation engine
- services
- controllers
- routes
- middleware
- utilities

DO NOT create duplicate models or duplicate business logic if an equivalent already exists.

If an API already exists, connect the Admin frontend to it.

Only create/modify APIs where required.

The Admin frontend should never directly access PostgreSQL.

Architecture must remain:

React Frontend
      ↓
Node.js + Express API
      ↓
Services / Allocation Engine
      ↓
PostgreSQL

======================================================
11. DATA CONSISTENCY
======================================================

This is a critical requirement.

There must be ONE source of truth: PostgreSQL.

For example:

Shelter:
capacity = 800
occupied = 210
free = 590

If an allocation of 18 people is confirmed:

occupied/free values must update consistently.

The same updated values must appear in:

- Dashboard
- Shelters
- Allocation review
- Requests where relevant
- History

Do not maintain independent fake state for the same information in different pages.

Avoid:

Dashboard data ≠ Shelter data
Shelter data ≠ Allocation data
Allocation data ≠ Database

Everything must remain synchronized.

======================================================
12. UI PRESERVATION
======================================================

The screenshots are the visual reference.

Preserve:

- Header
- Navbar
- Active navigation indicator
- Page widths
- Tables
- Cards
- Modal layouts
- Buttons
- Typography
- Borders
- Status badges
- Light theme
- Dark theme
- Spacing
- Alignment
- Responsive behavior

Do not introduce blue-heavy styling.

Do not change the approved red/orange/neutral visual language.

Do not add unnecessary animations.

Do not replace the design with Material UI, Bootstrap, Tailwind components, or another design system unless the existing project already uses it and it does not alter the approved appearance.

======================================================
13. RESPONSIVENESS
======================================================

The Admin pages must remain usable on:

- Desktop
- Laptop
- Tablet
- Smaller screens

Tables/modals should not overflow incorrectly.

Large modals must scroll internally where necessary.

Maps must remain correctly sized.

======================================================
14. REMOVE PROTOTYPE BEHAVIOR
======================================================

Search the Admin implementation for:

- alert(...)
- console-only actions
- fake demo data
- hardcoded statistics
- placeholder maps
- fake buttons
- "will be implemented later"
- "connect to backend later"
- simulated allocation actions
- fake exports

Replace these with real implementations.

Do not leave prototype behavior hidden behind the UI.

======================================================
15. ERROR / LOADING STATES
======================================================

Add proper:

- Loading states
- Empty states
- API error states
- Validation errors
- Success feedback
- Confirmation for destructive/important actions

Do this without changing the approved visual design.

======================================================
16. TESTING
======================================================

After implementation, test the complete Admin workflow.

At minimum verify:

AUTH
- Admin login
- Admin route protection
- User cannot access Admin APIs
- Logout

DASHBOARD
- Statistics load from database
- Shelter capacity is correct
- Requests needing attention are correct

SHELTERS
- Search
- Filter
- View
- Edit
- Add shelter
- Capacity calculation
- Status changes

REQUESTS
- Search
- Filters
- Open request
- Full request details
- Real coordinates
- Real map

ALLOCATIONS
- View allocation
- Review allocation
- Keep allocation
- Change shelter
- Capacity updates
- Request status updates
- Allocation status updates
- History creation

HISTORY
- Records appear
- Search
- Filter
- Details
- Operational context
- Excel export

THEMES
- Light mode
- Dark mode
- No unreadable text
- No broken borders/buttons/maps

======================================================
17. DATABASE INTEGRITY
======================================================

Pay particular attention to concurrent allocation/capacity updates.

If two requests are allocated to the same shelter, the backend must prevent capacity from becoming inconsistent or negative.

Use proper database transactions where required.

Allocation operations should be atomic:

BEGIN
  validate shelter capacity
  update allocation
  update shelter occupancy
  update request status
  insert history record
COMMIT

If anything fails:

ROLLBACK

Do not leave partially updated allocation state.

======================================================
18. DO NOT BREAK EXISTING ESAP FEATURES
======================================================

This is an existing project.

Before changing anything:

1. Inspect the repository structure.
2. Inspect package.json files.
3. Inspect frontend architecture.
4. Inspect backend architecture.
5. Inspect PostgreSQL schema/migrations.
6. Inspect authentication.
7. Inspect allocation engine.
8. Inspect existing API routes.
9. Inspect existing tests.

Do not blindly rewrite the project.

Make the smallest clean architectural changes necessary.

Preserve working functionality.

======================================================
19. IMPLEMENTATION PROCESS
======================================================

Follow this sequence:

PHASE 1
Audit the existing codebase.

Identify:
- frontend entry points
- Admin components/pages
- backend routes
- database schema
- allocation engine
- authentication
- existing tests

PHASE 2
Map each screenshot/HTML feature to the corresponding existing code.

Create a clear internal mapping:

Dashboard → API/service
Shelters → API/service
Requests → API/service
Allocations → API/service
History → API/service

PHASE 3
Connect existing frontend components to real APIs.

PHASE 4
Implement missing backend functionality.

PHASE 5
Integrate Leaflet/OpenStreetMap properly.

PHASE 6
Implement real allocation confirmation/reassignment.

PHASE 7
Implement audit/history persistence.

PHASE 8
Implement proper Excel export.

PHASE 9
Run backend tests.

PHASE 10
Run frontend/build/type checks.

PHASE 11
Manually verify every Admin workflow.

======================================================
20. IMPORTANT DEVELOPMENT RULE
======================================================

DO NOT STOP AFTER MAKING THE UI LOOK CORRECT.

The UI already looks approximately correct.

The main objective now is:

"MAKE THE EXACT APPROVED ADMIN UI FULLY FUNCTIONAL WITH THE REAL ESAP BACKEND."

Every visible button that represents an operation must actually perform that operation.

Every displayed data value must have a valid source.

Every operation that changes system state must persist to PostgreSQL.

======================================================
21. FINAL ACCEPTANCE CRITERIA
======================================================

Consider the Admin module COMPLETE only when:

✓ Dashboard matches the approved screenshots
✓ Shelters matches the approved screenshots
✓ Requests matches the approved screenshots
✓ Allocations matches the approved screenshots
✓ History matches the approved screenshots
✓ Light mode works
✓ Dark mode works
✓ Real PostgreSQL data is displayed
✓ Admin authentication works
✓ Admin authorization works
✓ Shelter CRUD works
✓ Request details work
✓ Real Leaflet/OpenStreetMap maps work
✓ Allocation review works
✓ Keep allocation works
✓ Change shelter works
✓ Capacity remains consistent
✓ History/audit records are persisted
✓ History details work
✓ Excel export produces a real .xlsx file
✓ Search/filter functionality works
✓ Loading/error/empty states work
✓ No hardcoded demo data remains where backend data is expected
✓ No fake map remains
✓ No prototype alerts remain
✓ Existing ESAP functionality is not broken
✓ Backend tests pass
✓ Frontend build/type checks pass

======================================================
FINAL INSTRUCTION
======================================================

First inspect the existing codebase thoroughly.

DO NOT immediately start rewriting files.

Understand the existing architecture first.

Then implement the Admin integration systematically.

Use the provided screenshots and HTML as the exact UI/UX reference.

The Admin design is LOCKED.

Do not redesign it.

Make it REAL.
Make it CONNECTED.
Make it DATA-DRIVEN.
Make every interaction actually work.

When finished, report:

1. Files changed
2. APIs added/modified
3. Database changes
4. Admin features implemented
5. Map implementation
6. Allocation workflow implementation
7. History/export implementation
8. Tests executed and results
9. Any remaining issues

Do not claim completion unless you actually verified the implementation.